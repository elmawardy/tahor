version: '3.4'
services:
  redis:
    image: "redis:6.0.6"
  postgres:
    build:
      context: .
      dockerfile: postgresql.dockerfile

  test-web-backend:
    depends_on: 
      - postgres
      - redis
    
    build:
      context: .
      dockerfile: dev-backend.dockerfile