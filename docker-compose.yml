version: '3.4'
services:
  redis:
    image: "redis:6.0.6"
  postgres:
    build:
      context: .
      dockerfile: postgresql.dockerfile

  # web-backend:
  #   build: 
  #     context: .
  #     dockerfile: backend.dockerfile

  #   depends_on: 
  #     - redis
  #     - postgres

  #   ports:
  #     - "8000:8000"

  #   # tty: true

  dev-web-backend:
    depends_on: 
      - postgres
      - redis
    build:
      context: .
      dockerfile: dev-backend.dockerfile